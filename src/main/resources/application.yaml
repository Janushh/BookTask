spring:
  application:
      name: BookTask

  datasource:
    url: jdbc:mysql://localhost:3306/books?useSSL=false&serverTimezone=UTC
    username: root
    password: qwerty123456

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
